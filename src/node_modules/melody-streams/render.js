import { render as mount, h } from 'preact';

export const render = (el, Component, props) => {
    mount(h(Component, props), el);
};

export const unmountComponentAtNode = el => {
    // TODO implement unmounting
};
